name: regent
description: "Optimistic, message-driven state engine: a journal of sealed messages posted into keyed registries and paginated connections. Pure Dart."
version: 0.0.1
repository: https://github.com/CardosoShlomo/regent

environment:
  sdk: ^3.12.0

dependencies:
  identifiable: ^0.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.31.0
