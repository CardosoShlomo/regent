name: regent
description: "Optimistic, message-driven state engine: a journal of sealed facts folded into keyed stores and units, with guards, correlation, and merge edges. Pure Dart."
version: 0.5.0
repository: https://github.com/CardosoShlomo/regent

environment:
  sdk: ^3.12.0

dependencies:
  identifiable: ^0.5.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.31.0
